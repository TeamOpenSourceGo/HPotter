docker stop $(docker ps -a -q)

docker rm $(docker ps -a -q)

docker system prune -a

docker rmi $(docker images -a -q)
---
docker build -t debian:telnetd2 .
docker build --no-cache -t debian:telnetd2 .

docker run --hostname credentials_server -p 23:23 debian:telnetd2
